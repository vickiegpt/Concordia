[package]
name = "pacc_runtime_sys"
version = "0.1.0"
authors = ["ZLUDA Team"]
edition = "2021"
description = "Rust bindings for PACC Runtime (SiFive Intelligence XM / RISC-V IME)"
license = "MIT"

[lib]
crate-type = ["rlib", "staticlib"]

[dependencies]
tempfile = "3.8.0"
libc = "0.2"

[build-dependencies]
